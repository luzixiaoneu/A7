pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    include ':spotify-app-remote'
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }

}
rootProject.name = "spotify_test1"
include ':app'
